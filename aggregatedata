sed 's/‾/	/g'| sort -t'	' -k 2,2 -k 3,3| awk -F'	' '{OFS="‾"; all=$0; last=$NF; NF=NF-1; key=$0; $0=all; if(NR > 1 && key!=previouskey){printf "%s",previouskey; for(value in values) { printf "‾‾‾%s",value } ; delete values;print "";} previouskey = key; values[last]=1; }'
